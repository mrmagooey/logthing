[package]
name = "loadgen"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
logthing = { path = "../.." }
tokio = { version = "1.35", features = ["full"] }
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"
